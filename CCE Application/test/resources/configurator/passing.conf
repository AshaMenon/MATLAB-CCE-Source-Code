# This is a well formed configuration file. Use this in all tests.
[CalculationDB]
    ServerName = "ons-opcdev.optinum.local"
    DBName = "wacs"
[CoordinatorDB]
    ServerName = "ons-opcdev.optinum.local"
    DBName = "wacs"
[Configurator]
    # TetMode - Tell the configurator to load but not write anything but a log file
    TestMode = 1 
    LogFile = "%CCE_CONFIG_PATH%\logs\configurator.log"
[Coordinator]
    MaxCalculationLoad = 20